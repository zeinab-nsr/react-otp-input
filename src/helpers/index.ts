// export function splitStringToChunks(
// 	str: string,
// 	by: number,
// 	prev: Array<string> = [],
// ): Array<string> {
// 	if (str.length === 0) {
// 		return prev;
// 	}

// 	prev.push(str.substring(0, by));
// 	return splitStringToChunks(str.substring(by), by, prev);
// }
